## Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

## Secrets
.env
*.key
*.pem

## Build/Artifacts
dist/
build/
*.log
output/
runtime/bin/
runtime/models/
runtime/tmp_pipeline/
cache/
crashes/

## Editors/OS
.vscode/
.idea/
.vs/
.cursor/
.claude/
.DS_Store

## Local binaries (don’t commit compiled outputs)
*.exe
*.dll
*.pdb
*.lib
*.exp

## Data/Models/Audio (don’t push these)
data/
datasets/
checkpoints/
*.pt
*.bin
*.onnx
*.ckpt
*.wav
*.flac
*.mp4
# The site's hero demo is a shipped asset, not a local recording. Without this
# exception it stays untracked, Vercel never deploys it, and /demo.mp4 404s
# behind a working poster frame.
!web/public/demo.mp4
*.zip
*.7z
*.tar
*.tar.gz
!src/whisper_local/message-send.mp3

## Local run outputs
whisper_stats.json
flow_out.txt

## Local backups
.git.backup/

# Local test run artifacts
pytest_out*.txt

# Auto-generated build config (contains baked secrets)
src/whisper_local/_build_config.py
ui-ux-pro-max-skill/

# The GitHub Support ticket for the history purge. Names the exact commits where
# the orphaned blobs are still fetchable, so committing it to this public repo
# would publish a map to the leak it exists to close. Keep it local.
docs/github-support-request.md
